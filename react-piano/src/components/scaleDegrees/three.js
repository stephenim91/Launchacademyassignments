import React from 'react';
import Middlewhitekey from '../../images/middlewhitekey.js';

const Three = (props) => {
  return(
    <span onClick={props.click}>
      <Middlewhitekey/>
    </span>
  )
}

export default Three;
