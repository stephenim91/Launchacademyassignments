import React from 'react';
import Leftwhitekey from '../../images/leftwhitekey.js';

const One = (props) => {
  return(
    <span onClick={props.click}>
      <Leftwhitekey/>
    </span>
  )
}

export default One;
